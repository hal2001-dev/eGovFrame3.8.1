# --- build output ---
target/
*.war
*.class

# --- distributable bundle (redundant; jars are committed under m2-repo) ---
*.zip

# --- OS / IDE ---
.DS_Store
*.iml
.idea/
.settings/
.project
.classpath
.factorypath

# --- logs ---
*.log

# NOTE:
#  m2-repo/ IS committed on purpose (offline dependency cache).
#  Do NOT commit real credentials: keep oracle.properties password and any real
#  HMAC secrets out of git (use deploy-time values / a local override).
